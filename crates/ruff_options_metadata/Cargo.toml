[package]
name = "sightline-ruff-options-metadata"
version = "0.1.0"
description = "sightline's fork of ruff_options_metadata, an internal crate of ruff and ty (astral-sh/ruff), as the sightline binary pins it"
authors = { workspace = true }
edition = { workspace = true }
rust-version = { workspace = true }
homepage = "https://github.com/Skyway1111/ty-unnecessary"
repository = "https://github.com/Skyway1111/ty-unnecessary"
license = { workspace = true }
exclude = ["tests/**", "resources/**", "benches/**", "fuzz/**", "**/snapshots/**"]

[dependencies]
serde = { workspace = true, optional = true }

[lints]
workspace = true

[lib]
name = "ruff_options_metadata"
test = false
